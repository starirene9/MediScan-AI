/**
 * Active color theme.
 *
 * Switch back to the previous palette anytime:
 *   export const ACTIVE_THEME = "legacy";
 *
 * Legacy CSS snapshot: src/theme/legacy.css.backup
 */
export type ThemeId = "legacy" | "clinical-slate";

export const ACTIVE_THEME: ThemeId = "legacy";
