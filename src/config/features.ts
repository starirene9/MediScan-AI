/**
 * Application feature flags.
 */
export const FEATURES = {
  /** Reserved for a dedicated Grad-CAM API endpoint (currently bundled in /analyze). */
  ENABLE_GRADCAM_API: false,
} as const;

export const APP_NAME = "MediScan AI";
